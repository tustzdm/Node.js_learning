1。核心模块
    文件操作的核心模块fs var fs = reuqire('fs');
    http服务构建模块http 
    路径操作模块path
    操作系统模块os

    要想使用必须用require引入

Node中没有全局作用域，只有模块作用域 

相对路径中的./不能省略



2.Ip地址用来定位地址，端口号用来定位具体的应用程序
    端口号不能重复占用
    